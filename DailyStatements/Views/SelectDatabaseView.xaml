<!-- There's code-behind functionality going on here -->
<Window x:Class="AMPStatements.Views.SelectDatabaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:AMPStatements.ViewModels"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    Title="AMP Statements" Height="151" Width="258" WindowStartupLocation="CenterScreen">
    <Grid>
        <ComboBox x:Name="comboBox1" ItemsSource="{Binding Path=CompanyList}" DisplayMemberPath="CompanyName" SelectedItem="{Binding Path=SelectedCompany,Mode=TwoWay}" HorizontalAlignment="Left" Margin="12,40,0,0" VerticalAlignment="Top" Width="228" RenderTransformOrigin="0.483,1.5">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectCompanyCommand}" CommandParameter="{Binding SelectedItems, ElementName=comboBox1}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ComboBox>
        <Label Content="Select Database" HorizontalAlignment="Left" Margin="10,9,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
